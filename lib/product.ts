import product from "@/config/product.json";

/** Align UI copy with Edge Function secret `FREE_TIER_MONTHLY_AI_GENERATIONS` (default 3). */
export const FREE_TIER_MONTHLY_AI_GENERATIONS = product.freeTierMonthlyAiGenerations;
