import { TabEmptyState } from "@/components/shell/TabEmptyState";

/** Break habits list (Phase 5). Pink shell. */
export default function BreakScreen() {
  return (
    <TabEmptyState title="Break" variant="break">
      Track habits you are quitting. Same tools as Build, with a visual style tuned for breaking
      unwanted patterns.
    </TabEmptyState>
  );
}
